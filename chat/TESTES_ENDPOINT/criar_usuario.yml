info:
  name: criar_usuario
  type: http
  seq: 5

http:
  method: POST
  url: 127.0.0.1:8080/criar_usuario
  body:
    type: json
    data: |-
      {
        "nome": "estêvan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
