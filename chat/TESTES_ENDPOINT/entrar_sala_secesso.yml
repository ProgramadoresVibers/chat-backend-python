info:
  name: entrar_sala_secesso
  type: http
  seq: 3

http:
  method: POST
  url: 127.0.0.1:8080/entrar_sala
  body:
    type: json
    data: |-
      {
        "nome": "impacta"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
