info:
  name: login_sucesso
  type: http
  seq: 7

http:
  method: POST
  url: 127.0.0.1:8080/login
  body:
    type: json
    data: |-
      {
        "nome": "estêvan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
