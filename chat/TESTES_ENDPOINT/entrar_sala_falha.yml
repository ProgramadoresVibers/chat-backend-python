info:
  name: entrar_sala_falha
  type: http
  seq: 4

http:
  method: POST
  url: localhost:8080/entrar_sala
  body:
    type: json
    data: |-
      {
        "nome": "  "
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
