info:
  name: enviar_mensagem_falha
  type: http
  seq: 10

http:
  method: POST
  url: 127.0.0.1:8080/enviar_mensagem
  body:
    type: json
    data: |-
      {
        "texto": " ",
        "id_sala": 1,
        "id_usuario": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
