info:
  name: login_falha
  type: http
  seq: 6

http:
  method: POST
  url: 127.0.0.1:8080/login
  body:
    type: json
    data: |-
      {
        "nome": "  "
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
