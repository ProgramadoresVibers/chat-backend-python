info:
  name: login_falha
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8080/login
  body:
    type: json
    data: |-
      {
        "nome": "  "
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
